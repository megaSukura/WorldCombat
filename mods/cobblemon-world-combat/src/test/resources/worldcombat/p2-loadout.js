// Reuse the production loadout resolver with test-only mappings to the P2 mechanisms.
NativeLoadout.map("swift", "cobblemon_world_combat:bolt", function () { return 0; });
NativeLoadout.map("protect", "cobblemon_world_combat:ward", function () { return 0; });
NativeLoadout.map("rocktomb", "cobblemon_world_combat:wall", function () { return 0; });
NativeLoadout.map("quickattack", "cobblemon_world_combat:dash", function () { return 0; });
