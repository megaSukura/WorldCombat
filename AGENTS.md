# 项目协作规则

适用于本项目的开发、文件操作与文档维护。后续规则可在本文件继续追加。

## 1．不进行文件哈希验证

- 不主动计算、比对或记录文件哈希，不为复制、归档、编辑或版本确认生成哈希校验值、清单或报告。
- 文件检查按任务需要直接查看路径、内容或具体改动。简单文件操作保持简单，避免追加无实际价值的验证流程。
- 既有计划中涉及文件哈希验证的要求，以本规则为准，后续不再执行。

## 2．写文档时注意粉色大象／白熊效应

反复否定一个概念，也会让读者更关注这个概念。文档应把注意力放在希望读者理解和执行的内容上。

- 优先直接说明目标、结论、行动、责任和预期结果，使用具体、正向的表达。
- 省略无关的错误路线、假设风险和预防性辩解；避免为了衬托当前方案，先引入读者原本没有想到的反面情境。
- 修改段落时，先找出真正要传达的信息，再直接重写，减少围绕被否定概念展开的解释。
- 确实影响当前任务的限制、风险或历史差异，应集中、简短地说明，并联系具体做法；避免在多处反复强化。
- 例如，描述验证分工时可直接写：“技术检查按改动范围执行，操作体验由人工试玩。”

## 3．实现以代码为准，文档保持简洁

代码本身是实现最完整的一手文档。文档聚焦代码之外值得说明的信息，保持简洁、干净、直击要害。

- 代码已清楚表达、且会随实现变化的细节，不在文档中重复维护，例如类与函数清单、参数默认值、目录结构和逐步调用流程。
- 需要了解实现时，优先链接到对应代码或入口，让读者直接查看当前事实。
- 文档主要说明目标、设计理由、关键取舍、稳定约定和必要的使用步骤，以及代码本身难以解释的背景。
- 每段内容都应帮助读者理解问题或采取行动；删去重复说明和对代码的机械转述。

## 4．会打扰桌面的测试先确认

- Codex 执行会弹出游戏或其他应用窗口、抢占前台、操作键鼠或打断用户操作的测试前，先说明测试内容、预计窗口数量与时长、是否需要人工参与，取得用户确认后执行。
- 阶段计划审阅通过表示同意开发范围；可见或交互测试按用户确认的批次与时机安排。
- 构建、静态检查及确认不会弹窗或抢占前台的后台检查，按已授权范围执行。后台助手或专服保持隐藏，专服显式关闭 GUI。
- 用户暂不方便时，记录待验证项目，继续不依赖该结果的工作。

交付文档前，快速检查：读者是否能直接看出要做什么、为什么做、由谁做，以及下一步是什么；实现细节是否可以改为引用代码。

## 5．内容生成的范本须经用户验收

- 给后续 AI 的玩法、AI 行为、交互、平衡与表现范本，从用户已在游戏中验收认可的正式内容中选取，沿用其已认可的范围。
- 新增范本应是要保留的正式内容；完成用户试玩认可后，再列入创作指南或生成提示词。
- 测试夹具、模拟消费者和未经用户验收的新内容仅用于工程验证，不作为内容生成范本。接口与共享库可以作为实现资料，技术检查的结论限于实际验证的技术范围。

## 6．独立内容 Agent 的两项验证

- 独立内容 Agent 负责资料查询、设计和源码交付，验证有两项：
  - `node tools/check-unit.mjs <单元目录>`：按单元声明的依赖做类型检查，并校验贴图 id、中英文本键、状态效果图标与 MobEffect id。只读、秒级。
  - `python tools/smoke-unit.py <单元目录> [<单元目录> ...]`：把共享包和这些单元装配成私有内容，在一台隐藏的无头专服上依次跑每个单元自己的 `scenario.ts`（布置场面、AI 交战、少量必然事实的断言、作者的 `note`），每个单元一条判定与一份轨迹（`build/smoke/<名>/trace.jsonl`、`server.log`）。开服约 35 秒，之后每个场景约 15 秒，占 2 GB 内存，不弹窗；一位作者负责几招就一次传几个目录。场景之间各用一块新场地，互不影响。
- 两项都只写 `build/smoke/<名>` 与 `runs/smoke-<名>`，任意数量的作者可同时运行；作者不运行 gradle、不碰共享构建、不写临时自测程序。Java jar 由集成者预先构建。
- 集成者按实际改动范围统一验证，并向用户交付人工体验检查；可见测试仍遵守第 4 条。

## 7．子代理调用方式

- 协调者按任务的推理难度、耦合程度与批量成本选择子代理，续作与复核也重新判断：
  - 边界明确、任务卡成熟、可独立重复的批量内容或机械修改，用 OpenCode 的 `batch_task`；省略 `agent` 字段，显式指定 `model: "deepseek/deepseek-flash"`，发挥低成本与高并发优势。
  - 架构取舍、跨层根因定位、复杂机制推理、玩法创意与质量审查，用 Codex 原生子代理及适合复杂推理的模型。默认继承主代理模型；需要覆盖时明确选择。
  - 批量结果出现系统性遗漏或任务依赖未理清时，由协调者或 Codex 原生子代理先核实并完善任务边界，再交回批量执行。
- 内容生产的清单统计、排序、分组与覆盖校验由协调者和本机脚本完成；内容创作按每位作者数招拆成独立任务后再并行派发。
- 内容试点用于检验正式任务卡。发现生产偏差时，先修正任务卡或已确认的共享能力缺口，再重新检验生成结果。
- 新批次与续作使用当前作者入口和任务卡；历史展开的批量模板用于追溯，派发前重新生成通用要求。

## 8．共享能力按通用契约扩展

- 扩展共享层时检查同类事实、操作、策略与生命周期，形成可供独立内容复用的接口；具体玩法由内容单元定义。
- 源码、注释、SDK 和作者资料直接说明当前通用契约与使用入口；反馈过程与设计讨论放入归档资料。技术回归使用中性夹具。

## 9．体验反馈按证据与成本限定范围

- 处理内容反馈前阅读[体验反馈与有限范围改进](docs/authoring/feedback.md)。用户按正常游玩反馈局部体验，协调者负责定位并明确本轮目标、修改范围和预期变化。
- 以已有交付和验证为基础；扩大修改与回归范围须有具体根因、调用影响及当前成本依据。每轮按其目标完成情况收尾，用户认可范围如实记录。
- 反馈原文、处理范围、实际结果与待处理意见落盘，后续会话和新接手作者从记录继续。
