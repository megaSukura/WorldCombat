# 0.1.0-alpha.1 发布准备记录

项目所有者已确认 GPL-3.0-or-later 与 `LICENSE-MINECRAFT-EXCEPTION.txt` 的额外许可，并授权新建公开仓库 [megaSukura/WorldCombat](https://github.com/megaSukura/WorldCombat)。首版标签为 `v0.1.0-alpha.1`，安装包与完整源码同版分发。

## 已完成

- 开始准备前，完整工作状态已保存在本地 Git 提交 `4e52a474`。
- 玩家包采用标准 `mods/` 与 `kubejs/` 布局，包含正式 play 的 800 个包/单元（756 招式、24 特性及共享内容）；开发 profiles/SDK 另行组装。
- 安装说明、版本记录、第三方通知、GPL 及依赖许可正文已准备。每个自有 jar 内也包含本项目许可与通知。
- 13 张小图标已追溯到本项目作者的独立程序绘图；两份绘图源码保存在 `tools/artwork/`。
- 本地历史保留。公开源码快照按项目源码范围导出，不包含旧 Photon/Unity 商业素材归档、Git 历史、缓存、存档或外部依赖 jar。
- 修正 8 处中英文参数占位不一致，删除纯表现文案残留；没有改动招式机制。

## 实际验证

首次完整 Gradle 检查完成所有单元依赖隔离的类型检查后，在中英文说明一致性处发现上述问题。修正 JSON 后重新装配所有正式 profiles，再完成其余通用检查与发行组装。已完成的类型检查未重复执行；详细日志保留在本地 `build/release-prep/`。

官方 NeoForge 21.1.250 安装器创建的独立无 GUI 专服，使用实际发行 jar、正式 play 脚本和官方依赖发行包正常启动、保存并停止，退出码 0，未使用开发类目录。结果在本地 `build/release-prep/installed-20260922-134008/result.json`。Kotlin for Forge 使用实际玩家下载的 `kotlinforforge-5.12.0-all.jar`；T88 的版本范围已按 FML 兼容矩阵核实。

原生 UI、粒子、PP/配置、地形生命周期的已有检查继续作为各自范围的依据。首版多客户端人工联机与动态观感未在本轮追加验证；已知体验范围明确列在 `CHANGELOG.md`。

## 公开边界

公开历史从清洁源码开始，本地开发检查点保留在私有工作目录；这里提及的本地检查点并不属于公开提交历史。第三方 Mod 继续通过官方渠道分发，旧商业素材及个人运行数据不进入公开仓库。
